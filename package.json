{
  "name": "scratch-system-repo",
  "private": true,
  "description": "Next.js app lives in ./web (Vercel Root Directory must be web). This file has no next dependency on purpose.",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "start": "npm run start --prefix web",
    "lint": "npm run lint --prefix web"
  }
}
